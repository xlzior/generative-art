pre-commit:
  parallel: true
  jobs:
    - name: biome format & lint
      run: pnpm biome check --write {staged_files}
      glob: "*.{js,ts,tsx,jsx,json,css}"
      stage_fixed: true
    - name: type check
      run: pnpm tsc --noEmit
